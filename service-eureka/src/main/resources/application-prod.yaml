eureka:
  server:
    enable-self-preservation: true
  instance:
    hostname: ${EUREKA_HOST}
management:
  server:
    port: 9000
  tracing:
    enabled: true
  otlp:
    tracing:
      connect-timeout: 10s